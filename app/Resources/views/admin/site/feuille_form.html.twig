{% extends 'admin/base_with_header.html.twig' %}

{% form_theme form 'form_theme_admin.html.twig' %}

{% block content %}

    <h2>{{ formTitle }}</h2>

    {{ form_start(form) }}

        <div class="ui segment">
            <h2 class="ui header">Feuille</h2>
            <div class="ui clearing divider"></div>
            <div>
                <div class="ui form">
                    {{ form_row(form.nom) }}
                    {{ form_row(form.lien) }}
                    {{ form_row(form.alt) }}
                    {{ form_row(form.image) }}
                    {% if icone  %}
                        <span>Actuellement : {{ icone }}</span>
                        <img src="{{ icone }}" >
                    {% endif %}
                </div>
            </div>
        </div>

        <div class="ui segment">
            <h2 class="ui header">Méta-données</h2>
            <div class="ui clearing divider"></div>
            <div>
                <div class="ui form">
                    {{ form_row(form.date) }}
                     {{ form_row(form.position) }} 
                    {{ form_row(form.etat) }}
                    {{ form_row(form.patterns) }}
                </div>
            </div>
        </div>
        <div class="ui segment">
            <div>
                <div class="ui form">
                    <div class="inline fields ui grid">
                        <div class="three wide column">                            
                        </div>
                        <div class="three wide column">
                            <input type="submit" value="Enregistrer" class="ui primary button">
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="ui segment">
                <p>
                    <span class="obligatoire">*</span> indique un champ obligatoire
                </p>
        </div>

    {{ form_end(form) }}

{% endblock %}
